import React from "react";
import styled from "styled-components";

const LogoEl = styled.div`
  margin-bottom: 32px;

  color: #f3f4f0;
  font-weight: 800;
`;

const Logo: React.FC = () => {
  return <LogoEl></LogoEl>;
};

export default Logo;
